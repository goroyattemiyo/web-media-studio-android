# HANDOFF 2026-09-11 — WMS Android Gate A0

## Canonical repository / branch

- Repository: `goroyattemiyo/web-media-studio-android`
- Current branch: `feat/gate-a0-bootstrap`
- PR: `#1 feat: bootstrap Android Gate A0 local acquisition spike`
- Gate A0 real-device feasibility is now **PASS**.
- PR #1 can be merged after the latest branch CI is green.

## Product direction

WMS Android is the main mobile implementation for:

`Share URL -> local acquisition -> Local Library -> Playlist -> background / screen-off playback`

The app is development-distributed only for now; Play Store publication is not a requirement.

Supported-source ambition is broader than YouTube: public URLs such as YouTube, TikTok, Instagram Reels and other yt-dlp-compatible sites, subject to rights/permission checks and without DRM/auth bypass.

## Gate A0 goal and result

Goal:

`URL input/share -> Probe -> save MP3 locally -> Media3 playback`

Result on the target Android device:

**PASS**

Verified path:

1. APK installs and starts.
2. Acquisition Engine = `READY`.
3. Explicit `Update yt-dlp stable` succeeds.
4. Active yt-dlp version = `2026.08.19`.
5. YouTube Probe succeeds and returns title/provider.
6. Rights/permission confirmation can be enabled.
7. `Save audio / MP3 192 kbps` completes.
8. A non-empty MP3 is created under app-specific Android storage.
9. Media3 local preview opens the saved file.
10. Playback succeeds; UI shows `Pause` while playing.

Successful real-device sample title:

`Michael Jackson - Beat It (Official 4K Video)`

Therefore:

- A0-1 runtime initialization: **PASS**
- A0-2 metadata Probe: **PASS**
- A0-3 real media acquisition: **PASS**
- A0-4 produced-file Media3 playback: **PASS**
- Gate A0 overall: **PASS**

## Important diagnostic conclusion

Earlier builds returned:

`SOURCE_FORBIDDEN`

for the same YouTube acquisition path while the active yt-dlp was stale.

After explicitly updating the active yt-dlp to `2026.08.19`, the tested YouTube sample completed acquisition and playback successfully.

This means stale yt-dlp was materially involved in the earlier failure for this sample. Do not add QuickJS/PO-token/player-client work merely because the older build produced HTTP 403; only add those integrations if future provider-specific diagnostics prove they are needed.

## Current implementation

- Kotlin + Jetpack Compose
- Media3 / ExoPlayer local preview
- `youtubedl-android` + FFmpeg acquisition engine
- `MediaAcquisitionEngine` abstraction
- Android share target for `text/plain`
- URL validation
- rights/permission confirmation
- MP3 192 kbps Gate A0 path
- 30-minute Gate A0 duration limit
- one acquisition process at a time
- sanitized/classified error display
- active yt-dlp version display
- explicit stable yt-dlp update action
- optional sanitized `yt-dlp Diagnostics` action

## Product/security boundaries

- permitted/public or otherwise authorized media only
- local device acquisition
- no account cookies in MVP
- no proxy rotation
- no DRM bypass
- no authentication/access-control bypass
- no arbitrary yt-dlp flags exposed through UI

## Next step after merge

Proceed from feasibility spike to productization:

1. Gate A1 — harden Android share intake / Import sheet.
2. Gate A2 — move acquisition into a managed foreground job/service with progress and cancel.
3. Gate A3 — persistent Local Library with Room.
4. Gate A4 — persistent playlists.
5. Gate A5 — MediaLibraryService / MediaSession / screen-off playback.
6. Gate A7 provider matrix — continue TikTok, Instagram and direct-media validation separately.

Do not confuse one successful public YouTube sample with universal provider support. Gate A0 proves the Android local-acquisition architecture is viable; provider support remains an explicit test matrix.
